% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linear_reg.R
\name{linear_reg}
\alias{linear_reg}
\title{Test for a Linear Relationship Between Height and Weight}
\usage{
linear_reg(data, alpha = 0.05, plot = TRUE)
}
\arguments{
\item{data}{A data frame containing at least the columns \code{height} (numeric,
cm) and \code{weight} (numeric, kg).}

\item{alpha}{Significance level for the hypothesis test.  Default \code{0.05}.}

\item{plot}{Logical.  If \code{TRUE} (default) diagnostic plots are printed.}
}
\value{
An object of class \code{"lm_result"} (a named list) containing:
\itemize{
\item \code{hypotheses} — character vector stating H0 and H1
\item \code{model} — the fitted \code{lm} object
\item \code{coefficients} — data frame of coefficient estimates
\item \code{r_squared} — numeric R-squared value
\item \code{test_statistic} — numeric t-statistic for the slope
\item \code{df} — degrees of freedom
\item \code{p_value} — numeric p-value for the slope test
\item \code{alpha} — significance level used
\item \code{decision} — character: "Reject H0" or "Fail to reject H0"
\item \code{conclusion} — plain-English conclusion
\item \code{plots} — named list of ggplot objects (scatter, residuals, qq)
}
}
\description{
Fits a simple linear regression of \code{weight} on \code{height} and tests whether
the slope is zero.  Produces numerical and graphical assumption checks
(scatter plot with regression line, residuals-vs-fitted plot, and a normal
Q-Q plot of residuals).
}
\examples{
data(project2026_data)
result <- linear_reg(project2026_data)
print(result)

}
