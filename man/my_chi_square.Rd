% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/my_chi_square.R
\name{my_chi_square}
\alias{my_chi_square}
\title{Test for an Association Between Gender and Physical Activity}
\usage{
my_chi_square(data, alpha = 0.05, plot = TRUE)
}
\arguments{
\item{data}{A data frame containing at least the columns \code{gender}
(character or factor) and \code{phys} (character or factor with levels
\code{"None"}, \code{"Moderate"}, \code{"Intense"}; \code{NA} is treated as \code{"None"}).}

\item{alpha}{Significance level.  Default \code{0.05}.}

\item{plot}{Logical.  If \code{TRUE} (default) the bar chart is printed.}
}
\value{
An object of class \code{"chisq_result"} (a named list) containing:
\itemize{
\item \code{hypotheses} — character vector stating H0 and H1
\item \code{observed} — contingency table of observed counts
\item \code{expected} — matrix of expected counts under H0
\item \code{min_expected} — minimum expected cell count (assumption check)
\item \code{assumption_note} — character string summarising the assumption check
\item \code{test} — the \code{htest} object returned by \code{chisq.test()}
\item \code{test_statistic} — numeric chi-square statistic
\item \code{df} — degrees of freedom
\item \code{p_value} — numeric p-value
\item \code{alpha} — significance level used
\item \code{decision} — character: "Reject H0" or "Fail to reject H0"
\item \code{conclusion} — plain-English conclusion
\item \code{plots} — named list of ggplot objects (bar)
}
}
\description{
Performs Pearson's chi-square test of independence on a contingency table
of gender by physical activity level.  Missing values in the activity
column are recoded as \code{"None"} before testing.  Produces a stacked bar
chart and the expected-counts table as assumption checks.
}
\examples{
data(project2026_data)
result <- my_chi_square(project2026_data)
print(result)

}
