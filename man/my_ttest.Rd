% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/my_ttest.R
\name{my_ttest}
\alias{my_ttest}
\title{Test Whether Mean Height is the Same for Males and Females}
\usage{
my_ttest(data, alpha = 0.05, plot = TRUE)
}
\arguments{
\item{data}{A data frame containing at least the columns \code{height} (numeric,
cm) and \code{gender} (character or factor with levels \code{"Male"} and
\code{"Female"}).}

\item{alpha}{Significance level.  Default \code{0.05}.}

\item{plot}{Logical.  If \code{TRUE} (default) assumption plots are printed.}
}
\value{
An object of class \code{"ttest_result"} (a named list) containing:
\itemize{
\item \code{hypotheses} — character vector stating H0 and H1
\item \code{group_summary} — data frame of per-group means and SDs
\item \code{test} — the \code{htest} object returned by \code{t.test()}
\item \code{test_statistic} — numeric t-statistic
\item \code{df} — degrees of freedom
\item \code{p_value} — numeric p-value
\item \code{conf_int} — 95\% confidence interval for the difference in means
\item \code{alpha} — significance level used
\item \code{decision} — character: "Reject H0" or "Fail to reject H0"
\item \code{conclusion} — plain-English conclusion
\item \code{plots} — named list of ggplot objects (boxplot, qq)
}
}
\description{
Performs a two-sample independent t-test (equal variances assumed) to
compare the mean heights of male and female participants.  Produces a
boxplot and per-group normal Q-Q plots as assumption checks.
}
\examples{
data(project2026_data)
result <- my_ttest(project2026_data)
print(result)

}
